High performance compute requirements

- Needs for running QuantumRings SDK to generate data for runtime and memory usage, 
	- Needs both CPU and GPU, if hybrid possible too?
	- Be able to run/simulate 30-35 or even more qubits
	- Parallelization through High Throughput Computing to run multiple simulations at once
- Needs for training ML models (could be done with just GPU)

- Scalability: We need to understand baseline memory needs and be able to scale as time and/or more compute power needs
